[project]
name = "system1"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.136.3",
    "httpx==0.28.1",
    "numpy==2.4.6",
    "typer==0.26.7",
    "uvicorn==0.49.0",
]

[project.scripts]
system1 = "system1:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest==9.0.3",
    "ruff==0.15.16",
]
